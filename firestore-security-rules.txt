rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // ⚠️ WARNING: DEVELOPMENT MODE ONLY ⚠️
    // This allows ALL read and write access to your Firestore database.
    // DO NOT use this in production! This is a security risk.
    // For production, implement proper security rules based on user roles and permissions.
    
    // Allow all read and write access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
